# Local Data Directory

Downloaded datasets, generated Synthea records, model weights, and intermediate artifacts stay in this directory and are not committed.

Only this README and `.gitkeep` are tracked.

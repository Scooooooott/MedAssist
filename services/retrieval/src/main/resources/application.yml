spring:
  application:
    name: retrieval
server:
  port: 8084
logging:
  pattern:
    level: "%5p [trace_id=%X{trace_id:-}]"
management:
  endpoints:
    web:
      exposure:
        include: health
